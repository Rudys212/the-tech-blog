<div class="card text-white bg-dark mb-3 w-75 p-3">
    <div class="card-body">
        <p clasIs="card-text">{{comment_body}}</p>
        <div class="card-footer text-muted">
            <p>Posted by {{comments.user.username}} on {{format_date date_created}}</p>
        </div>
        </div>